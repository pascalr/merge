<p id="notice"><%= notice %></p>

<h1>Partials</h1>

<% @partials.each do |partial| %>
  <%= link_to partial.name, edit_partial_path(partial) %><br>
<% end %>
<%# link_to 'Destroy', partial, method: :delete, data: { confirm: 'Are you sure?' } %>

<br>

<h2>New partial</h2>

<%= form_with(model: Partial.new, local: true) do |form| %>
  
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
