<% @rendered_documents ||= {} %>
<% raise "Document already rendered. Preventing possible infinite loop" if @rendered_documents[document.id] %>
<% @rendered_documents[document.id] = true %>
<%= render inline: document.raw_content %>
